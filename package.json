{
  "name": "account-lookup-bot",
  "version": "2.0.0",
  "description": "Telegram bot — Vercel webhook deployment",
  "scripts": {
    "dev": "nodemon src/dev.js",
    "set-webhook": "node scripts/set-webhook.js",
    "delete-webhook": "node scripts/set-webhook.js --delete"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.66.0",
    "dotenv": "^16.4.5",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
